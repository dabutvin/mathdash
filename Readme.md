##Set up

1. clone the repo
`git clone https://github.com/dabutvin/mathdash.git`
`cd mathdash`

2. install dependencies
`npm install`

3. start grunt
`grunt`

4. start node
`npm start`

5. browse to localhost:3000

